import React from 'react';
import { View, StyleSheet, Text} from 'react-native'

const AcccountScreen = () => {
    return <Text style={{fontSize: 48}}> AcccountScreen </Text>;
};

const styles = StyleSheet.create({});

export default AcccountScreen;